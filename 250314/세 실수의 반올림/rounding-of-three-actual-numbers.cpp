#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double a, b, c;
    cin >> a >> b >>c;

    cout << fixed << setprecision(3) << a << endl;
    cout << fixed << setprecision(3) << b << endl;
    cout << fixed << setprecision(3) << c << endl;
    return 0;
}