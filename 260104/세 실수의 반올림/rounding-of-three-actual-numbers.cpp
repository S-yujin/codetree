#include <iostream>
using namespace std;

int main() {
    double a, b, c;
    cin >> a;
    cin >> b;
    cin >> c;

    cout << fixed;
    cout.precision(3);
    cout << a << endl << b << endl << c;
    return 0;
}